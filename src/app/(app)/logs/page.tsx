"use client";
import { useEffect, useState } from "react";
import { Badge, Card, PageHeader, Spinner } from "@/components/ui";
export default function LogsPage(){const[data,setData]=useState<any[]>();const[filter,setFilter]=useState("");useEffect(()=>{fetch("/api/logs").then(r=>r.json()).then(setData)},[]);const rows=(data||[]).filter(x=>`${x.action} ${x.companyName} ${x.hrEmail} ${x.message}`.toLowerCase().includes(filter.toLowerCase()));return <><PageHeader title="Activity logs" description="An auditable trail for every automated and user-triggered action."/><Card className="overflow-hidden"><div className="p-4"><input className="input max-w-lg" placeholder="Filter action, company, email, or message…" value={filter} onChange={e=>setFilter(e.target.value)}/></div>{!data?<div className="grid h-60 place-items-center"><Spinner/></div>:<div className="table-wrap"><table><thead><tr><th>Timestamp</th><th>Action</th><th>Campaign</th><th>Company / HR</th><th>Status</th><th>User action</th><th>Message</th></tr></thead><tbody>{rows.map(l=><tr key={l.id}><td>{new Date(l.createdAt).toLocaleString()}</td><td>{l.action.replaceAll("_"," ")}</td><td>{l.campaign?.name||"—"}</td><td>{l.companyName||"—"}<small className="block text-gray-400">{l.hrEmail}</small></td><td><Badge tone={l.status==="SUCCESS"?"green":l.status==="FAILED"?"red":"yellow"}>{l.status}</Badge></td><td>{l.userAction?"Yes":"No"}</td><td className="max-w-lg whitespace-normal text-sm text-gray-500">{l.message}</td></tr>)}</tbody></table></div>}</Card></>}
